package main.log

/**
  * Modélise un log labellisé. Un log labellisé est un log, dont le label
  * est directement déterminé à partir du texte présent dans son contenu
  * et dans son entête.
  * La valeur est déterminée par une (ou plusieurs) règles, représentées
  * comme des fonctions.
  */
trait LabelledLog extends Log {

}
